{{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit"
            class="mt-4 inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white font-semibold text-sm rounded shadow transition">
        {{ button_label|default('Enregistrer') }}
    </button>

{{ form_end(form) }}