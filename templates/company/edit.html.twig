{% extends 'base.html.twig' %}

{% block title %}Modifier une entreprise{% endblock %}

{% block body %}
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8 bg-white shadow rounded-md">

    <h1 class="text-2xl font-bold leading-7 text-gray-900 mb-6">Modifier une entreprise</h1>

    {{ form_start(form) }}

    <div class="space-y-12">
        <div class="border-b border-gray-200 pb-12">
            <h2 class="text-base font-semibold leading-7 text-gray-900">Informations générales</h2>
            <p class="mt-1 text-sm leading-6 text-gray-600">Mettez à jour les informations de cette entreprise.</p>

            <div class="mt-10 grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-6">

                <div class="sm:col-span-4">
                    {{ form_label(form.name, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    <div class="mt-2">
                        {{ form_widget(form.name, {
                            'attr': {
                                'class': 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm placeholder:text-sm placeholder:text-gray-400 placeholder:italic',
                                'placeholder': 'Nom de l\'entreprise'
                            }
                        }) }}
                    </div>
                </div>

                <div class="sm:col-span-6">
                    {{ form_label(form.activity, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    <div class="mt-2">
                        {{ form_widget(form.activity, {
                            'attr': {
                                'class': 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm placeholder:text-sm placeholder:text-gray-400 placeholder:italic',
                                'placeholder': 'Secteur d\'activité'
                            }
                        }) }}
                    </div>
                </div>

                <div class="sm:col-span-6">
                    {{ form_label(form.address, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    <div class="mt-2">
                        {{ form_widget(form.address, {
                            'attr': {
                                'class': 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm placeholder:text-sm placeholder:text-gray-400 placeholder:italic',
                                'placeholder': 'Adresse complète'
                            }
                        }) }}
                    </div>
                </div>

                <div class="sm:col-span-2">
                    {{ form_label(form.postalCode, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    <div class="mt-2">
                        {{ form_widget(form.postalCode, {
                            'attr': {
                                'class': 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm placeholder:text-sm placeholder:text-gray-400 placeholder:italic',
                                'placeholder': '75000'
                            }
                        }) }}
                    </div>
                </div>

                <div class="sm:col-span-4">
                    {{ form_label(form.city, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                    <div class="mt-2">
                        {{ form_widget(form.city, {
                            'attr': {
                                'class': 'block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm placeholder:text-sm placeholder:text-gray-400 placeholder:italic',
                                'placeholder': 'Ville'
                            }
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-6 flex items-center justify-end gap-x-6">
        <a href="{{ path('app_company_index') }}" class="text-sm font-semibold text-gray-900 hover:underline">Annuler</a>
        <button type="submit" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500">
            {{ button_label|default('Enregistrer') }}
        </button>
    </div>

    {{ form_end(form) }}
</div>
{% endblock %}
